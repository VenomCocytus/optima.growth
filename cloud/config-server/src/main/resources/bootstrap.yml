spring:
  application:
    name: config-server

  profiles:
    active: native

  security:
    user:
      name: admin
      password: admin-password

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
#        git:
#          uri: https://github.com/your-github-username/your-config-repo.git
#          username: admin
#          password: admin
#          clone-on-start: true
#          force-pull: true
#          timeout: 5
#          search-paths: licensing-service

server:
  port: 8071

# Enable all spring boot actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"